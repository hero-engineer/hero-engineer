{
  "name": "@ecu/server",
  "version": "1.0.0",
  "description": "A particular system",
  "main": "index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "start:watch": "nodemon --watch src --ext ts,json --exec \"npm run start\"",
    "dev": "npm run clean:build && nodemon --watch src --ext ts,tsx --exec \"npm run build\"",
    "build": "npx tsc --declaration",
    "clean:build": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "apollo-server": "^3.8.1",
    "graphql": "^16.5.0",
    "typescript": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.16",
    "rimraf": "^3.0.2",
    "ts-node": "^10.8.0"
  }
}
