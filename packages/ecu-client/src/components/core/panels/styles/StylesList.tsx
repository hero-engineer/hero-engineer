import { ReactNode } from 'react'

type StylesListPropsType = {
  title: string
  items: ReactNode[]
  onAdd: () => void
}

function StylesList({ title, items, onAdd }: StylesListPropsType) {

  return (
    null
  )
}

export default StylesList
