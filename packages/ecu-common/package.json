{
  "name": "ecu-common",
  "version": "0.3.0",
  "description": "Shared helpers and types for Ecu",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run clean:build && nodemon --watch src --ext ts,tsx --exec \"npm run build\"",
    "build": "npx tsc --declaration",
    "clean:build": "rimraf dist",
    "prepublishOnly": "npm run clean:build && npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@babel/parser": "^7.19.1",
    "@babel/traverse": "^7.19.1",
    "@babel/types": "^7.19.0",
    "ecu-particule": "^3.0.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/babel__traverse": "^7.18.1",
    "@types/node": "^18.7.18",
    "@types/react": "^18.0.20"
  }
}
