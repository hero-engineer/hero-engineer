{
  "name": "photonfactory",
  "version": "1.0.0",
  "description": "A particular system",
  "main": "index.ts",
  "scripts": {
    "dev": "npm run clean:build && nodemon --watch src --ext ts,tsx --exec \"npm run build\"",
    "build": "npx tsc --declaration",
    "clean:build": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.16",
    "rimraf": "^3.0.2"
  }
}
