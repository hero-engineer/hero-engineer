{
  "name": "ecu-honorable",
  "version": "0.1.0",
  "description": "The ecu plugin for the honorable UI library",
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run clean:build && nodemon --watch src --ext ts,tsx --exec \"npm run build\"",
    "build": "npx tsc --declaration",
    "clean:build": "rimraf dist",
    "prepublishOnly": "npm run clean:build && npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@emotion/react": "^11.9.3",
    "@emotion/styled": "^11.9.3",
    "ecu-particule": "^1.1.0",
    "honorable": "^0.118.0",
    "honorable-theme-default": "^0.59.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.14"
  }
}
