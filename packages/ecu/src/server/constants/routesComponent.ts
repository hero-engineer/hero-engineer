import { ComponentType } from '../../types'

const routerComponent: ComponentType = {
  name: 'Routes',
  props: {},
  importName: 'Routes',
  importPath: 'react-router-dom',
  importType: 'named',
}

export default routerComponent
