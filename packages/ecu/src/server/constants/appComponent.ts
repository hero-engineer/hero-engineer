import { ComponentType } from '../../types'

const ecuComponent: ComponentType = {
  name: 'App',
  props: {},
  importName: 'App',
  importPath: '/App.tsx',
  importType: 'default',
}

export default ecuComponent
